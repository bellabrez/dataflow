from setuptools import setup, find_packages

setup(
    name="dataflow",
    version="0.0.1",
    long_description=__doc__,
    packages=['dataflow'],
    include_package_data=True,
    install_requires=[],
    zip_safe=False
)